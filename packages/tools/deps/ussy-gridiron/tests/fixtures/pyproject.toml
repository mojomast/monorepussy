[project]
name = "test-python-app"
version = "2.1.0"
dependencies = [
    "requests>=2.28.0",
    "pydantic>=1.10,<3.0",
    "click>=8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "black>=23.0",
]

[tool.poetry.dependencies]
python = "^3.9"
flask = "^3.0"
